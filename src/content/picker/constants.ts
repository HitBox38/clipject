/** Gap in pixels between the input element and the picker dropdown. */
export const PICKER_GAP = 4;

/** Minimum space (px) from any viewport edge. */
export const VIEWPORT_PADDING = 8;

/** Picker width in px — must match the CSS value in mount.ts. */
export const PICKER_WIDTH = 280;

/** Picker max-height in px — must match the CSS value in mount.ts. */
export const PICKER_MAX_HEIGHT = 320;
